{
  "name": "ai-study-kit",
  "private": true,
  "version": "0.1.0",
  "description": "AI-assisted study toolkit scaffold — turn any quiz set into a learning loop with courses, flashcards, wrong-question deep-dives, and spaced repetition.",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -C apps/quiz-app run dev",
    "build": "pnpm -C apps/quiz-app run build",
    "server": "pnpm -C apps/quiz-app run server",
    "start": "pnpm -C apps/quiz-app run start",
    "test": "pnpm -C apps/quiz-app run test",
    "scan": "python3 scripts/brand-scan.py"
  }
}
